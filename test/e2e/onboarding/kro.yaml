apiVersion: kro.services.open-control-plane.io/v1alpha1
kind: Kro
metadata:
  name: test-mcp
spec:
  # must be one of the versions offered by the ProviderConfig on the platform cluster
  version: "0.9.1"
